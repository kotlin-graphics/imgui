package imgui.impl

import javafx.application.Application
import javafx.stage.Stage
import kotlin.system.exitProcess

class OpenJFXDummyMain: Application() {
    override fun start(primaryStage: Stage?) { exitProcess(0) }
}